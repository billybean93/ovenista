# Content TODO

- Replace the local SVG placeholder assets in `/public/images` with real photography across the hero, dishes, gallery, story, and reservation backgrounds.
- Add the real phone number in `.env.local` as `NEXT_PUBLIC_PHONE`.
- Add the real street address in `.env.local` as `NEXT_PUBLIC_ADDRESS`.
- Add the real email in `.env.local` as `NEXT_PUBLIC_EMAIL`.
- Replace `NEXT_PUBLIC_MAPS_EMBED_ID` with the real Google Maps embed token or full embed query payload.
- Replace JSON-LD placeholder geo coordinates in [src/lib/metadata.ts](/Users/billybean/Documents/commercial-webs/ovenista/src/lib/metadata.ts).
- Replace social links in `.env.local`.
- Replace the placeholder review authors and dates if real reviews are approved for publication.
- Replace `/public/og-image.svg` with a production social card export.
- Add a real reservation submission flow for `RESERVATION_WEBHOOK_URL` or a server action / API route before launch.
